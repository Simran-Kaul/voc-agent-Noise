name: Weekly VoC Intelligence Agent

on:
  schedule:
    - cron: "0 2 * * 1"   # every Monday 02:00 UTC
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run VoC pipeline
        run: |
          python tools/flipkart_prodA.py
          python tools/flipkart_prodB.py
          python tools/analyze_reviews.py
          python tools/generate_report.py
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}

      - name: Upload VoC Reports
        uses: actions/upload-artifact@v4
        with:
          name: voc-reports
          path: |
            reports/global_voc_report.md
            reports/weekly_delta_analyse_report.md
            reports/weekly_delta_report.md
            data/reviews.db